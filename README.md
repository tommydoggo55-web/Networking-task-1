# Networking-task-1
Explored basic networking concepts and analyzed my device’s network using commands like `ipconfig` and `tracert`. Identified information such as IP address, default gateway, DNS, and network route, while gaining practical understanding of how devices connect and communicate over a network.
